import { useState } from 'react';
import './AddNiaz.css'


function CreateNiaz(){
  const [mode, setMode] = useState('default');

  return (
  <>
    <dev>
      {mode === 'default' && (
        <dev>
          
        </dev>
      )}
    </dev>
  </>)
}

export default CreateNiaz